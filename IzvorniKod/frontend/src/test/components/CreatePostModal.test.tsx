import { test, expect } from "vitest";
import { CreatePostModal } from "../../components/CreatePostModal/CreatePostModal";

test("answer component", async () => {
  expect(CreatePostModal).toBeTruthy();
});
