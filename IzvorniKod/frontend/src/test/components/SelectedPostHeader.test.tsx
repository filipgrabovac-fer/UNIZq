import { test, expect } from "vitest";
import SelectedPostHeader from "../../components/SelectedPostHeader/SelectedPostHeader";

test("answer component", async () => {
  expect(SelectedPostHeader).toBeTruthy();
});
